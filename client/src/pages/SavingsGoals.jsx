import React from 'react';
import { Typography } from '@mui/material';

const SavingsGoals = () => {
  return (
    <Typography variant="h4">Savings Goals Page</Typography>
  );
};

export default SavingsGoals;